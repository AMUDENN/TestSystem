<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="GridShadowStyle" TargetType="{x:Type Grid}">
        <Setter Property="Effect" Value="{DynamicResource dropShadow}"/>
    </Style>
    <Style x:Key="GridBackGroundStyle" TargetType="{x:Type Grid}">
        <Setter Property="Background" Value="{DynamicResource LightMain}"/>
    </Style>
    <Style x:Key="GridAdditionStyle" TargetType="{x:Type Grid}">
        <Setter Property="Background" Value="{DynamicResource Main}"/>
    </Style>
</ResourceDictionary>