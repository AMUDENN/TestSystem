<Window x:Class="TestSystem.MainWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestSystem.MainWindows"
        mc:Ignorable="d"
        Style="{DynamicResource WindowStyle}"
        Title="Автоматизированная тестовая система Демьянов"
        Height="1024" Width="1440">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="100"/>
            </Grid.RowDefinitions>
            <ContentControl Content="{Binding NavigationVM}" Grid.Row="0"/>
            <TextBlock Text="{Binding UserModel.CurrentUser.name}" Grid.Row="1"/>
        </Grid>
        <ContentControl Content="{Binding CurrentVM}" Grid.Column="1"/>
    </Grid>
</Window>
