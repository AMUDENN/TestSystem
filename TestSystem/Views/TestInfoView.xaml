<UserControl x:Class="TestSystem.Views.TestInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             xmlns:local="clr-namespace:TestSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="974" d:DesignWidth="1040">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <oxy:PlotView Model="{Binding PlotViewModel, Mode=OneWay}" Background="Transparent" Margin="30" Grid.Row="0"/>
        <Button Command="{Binding BackCommand}" Content="Назад" Style="{DynamicResource DarkMainColorButtonStyle}" Margin="40" Grid.Row="3"/>
    </Grid>
</UserControl>
