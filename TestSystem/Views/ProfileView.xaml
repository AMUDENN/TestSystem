<UserControl x:Class="TestSystem.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1024" d:DesignWidth="1440">
    <Grid>
        <Grid Margin="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="{Binding Name}" Style="{DynamicResource CommonTextBlockStyle}" Grid.Row="0"/>
            <TextBlock Text="{Binding Surname}" Style="{DynamicResource CommonTextBlockStyle}" Grid.Row="1"/>
            <TextBlock Text="{Binding Patronymic}" Style="{DynamicResource CommonTextBlockStyle}" Grid.Row="2"/>
            <TextBlock Text="{Binding Email}" Style="{DynamicResource CommonTextBlockStyle}" Grid.Row="3"/>
        </Grid>
    </Grid>
</UserControl>
