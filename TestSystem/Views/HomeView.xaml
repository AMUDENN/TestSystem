<UserControl x:Class="TestSystem.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="974" d:DesignWidth="1040">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Command="{Binding ExportCommand}" Content="Экспорт" Style="{DynamicResource DarkMainButtonStyle}" Margin="150, 400" Grid.Column="0"/>
        <Button Command="{Binding ImportCommand}" Content="Импорт" Style="{DynamicResource LightMainButtonStyle}" Margin="150, 400" Grid.Column="1"/>
    </Grid>
</UserControl>
